services:
  mongodb:
    image: mongo:7
    container_name: rotdash-mongodb
    environment:
      MONGO_INITDB_DATABASE: rotdash
    ports:
      - "27017:27017"
    volumes:
      - mongodb_data:/data/db
    restart: unless-stopped

volumes:
  mongodb_data:
